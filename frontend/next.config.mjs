/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        "ipfs.io", 
        "gateway.pinata.cloud",
        "cloudflare-ipfs.com",
        "infura-ipfs.io",
        "dweb.link",
        "thearchitecturedesigns.com",
        "media4.giphy.com"
      ],
    },
  };

export default nextConfig;
